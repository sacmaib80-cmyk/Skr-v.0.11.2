// ==========================================================
// TOI A1-019 — เหมือนกันหมด (All same / different / neither)
// ==========================================================
//
// 📋 โจทย์: รับเลข 3 ตัว แล้วแสดงผล
//    - "all the same"   ถ้าทั้ง 3 ตัวเท่ากันหมด
//    - "all different"  ถ้าทั้ง 3 ตัวต่างกันหมด
//    - "neither"        ถ้าเหมือนกันบางตัว
//
// ตัวอย่าง:
//    3, 3, 3  →  all the same
//    3, 4, 5  →  all different
//
// 🎯 ต้องใช้:  === , && , if / else if / else
// 🚫 ยังไม่ต้องใช้ function
//
// 💡 hint:
//    เท่ากันหมด =  a === b && b === c
//    ต่างกันหมด =  a !== b && b !== c && a !== c
// ----------------------------------------------------------

let a: number = 3;
let b: number = 4;
let c: number = 5;

// 👇
// ต้องได้:  all different
