// ==========================================================
// A1 — บทที่ 07: อาเรย์ (Array) — เก็บหลายค่าในตัวแปรเดียว
// ==========================================================
//
// 🎯 บทนี้ต้องใช้:
//   - ประกาศ array:  let nums: number[] = [1, 2, 3];
//   - เข้าถึงสมาชิกด้วย index (เริ่มนับจาก 0):  nums[0] คือตัวแรก
//   - .length  →  จำนวนสมาชิกใน array
//   - console.log(...)
//
// 🚫 บทนี้ยังไม่ใช้ function ที่เราเขียนเอง
//    แต่จะเริ่มเจอ "เมธอดของ array" ที่มีมาให้ → ดู hint
// ----------------------------------------------------------


// ── ตัวอย่าง ──────────────────────────────────────────────
let fruits: string[] = ["apple", "banana", "cherry"];

console.log(fruits[0]);      // apple   (ตัวแรก index 0)
console.log(fruits[2]);      // cherry  (ตัวที่ 3 index 2)
console.log(fruits.length);  // 3


// ── โจทย์ 1 ───────────────────────────────────────────────
// ประกาศ array ชื่อ  scores  เก็บตัวเลข 4 ตัว: 10, 20, 30, 40
// แล้ว log "ตัวสุดท้าย"
//   ต้องได้:  40
//
// 💡 hint: ตัวสุดท้ายคือ index = length - 1  →  scores[scores.length - 1]

// เขียนตรงนี้ 👇



// ── โจทย์ 2 ───────────────────────────────────────────────
// log ว่า array นี้มีสมาชิกกี่ตัว
let colors: string[] = ["red", "green", "blue", "yellow"];
//   ต้องได้:  4

// เขียนตรงนี้ 👇



// ── โจทย์ 3 ───────────────────────────────────────────────
// เพิ่ม "grape" เข้าไปท้าย array แล้ว log ทั้ง array
let basket: string[] = ["apple", "banana"];
//   ต้องได้:  [ 'apple', 'banana', 'grape' ]
//
// 💡 hint (เมธอดใหม่ที่ต้องใช้ ไม่งั้นเพิ่มของไม่ได้):
//    .push(x)  →  เพิ่ม x เข้าไปท้าย array
//    ตัวอย่าง:  basket.push("grape")

// เขียนตรงนี้ 👇



// ── โจทย์ 4 ───────────────────────────────────────────────
// หา "ผลรวม" ของเลข 3 ตัวใน array (บวกทีละตัวด้วย index)
let data: number[] = [5, 10, 15];
//   ต้องได้:  30
//
// 💡 hint: data[0] + data[1] + data[2]

// เขียนตรงนี้ 👇



// ── โจทย์ 5 ───────────────────────────────────────────────
// สลับ log ตัวแรกกับตัวสุดท้ายของ array นี้
let queue: string[] = ["A", "B", "C", "D"];
//   ต้องได้:  D  (ตัวสุดท้าย) แล้วบรรทัดถัดมา  A  (ตัวแรก)

// เขียนตรงนี้ 👇
