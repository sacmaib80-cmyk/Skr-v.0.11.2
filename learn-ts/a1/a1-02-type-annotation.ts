// ==========================================================
// A1 — บทที่ 02: กำหนด Type (Annotation) กับให้ TS เดาเอง (Inference)
// ==========================================================
//
// 🎯 บทนี้ต้องใช้:
//   - Type Annotation   →  let x: number = 5;   (บอก type ตรงๆ)
//   - Type Inference    →  let x = 5;            (TS เดา type ให้เอง)
//   - console.log(...)
//
// 🚫 บทนี้ยังไม่ใช้ function
//
// จุดสำคัญของ TS: พอกำหนด type แล้ว เอา type อื่นมาใส่ไม่ได้ (Error)
// ----------------------------------------------------------


// ── ตัวอย่าง ──────────────────────────────────────────────
let count: number = 10;   // กำหนด type ตรงๆ
let title = "Hello";      // TS เดาเองว่าเป็น string

// count = "ten";  // ❌ Error — เพราะ count เป็น number
console.log(count, title); // 10 Hello


// ── โจทย์ 1 ───────────────────────────────────────────────
// ประกาศ  price  โดย "กำหนด type ตรงๆ" ให้เป็น number = 99
//   ต้องได้:  99

// เขียนตรงนี้ 👇



// ── โจทย์ 2 ───────────────────────────────────────────────
// ประกาศ  message  โดย "ให้ TS เดาเอง" (ไม่ต้องใส่ : string)
// เก็บค่า "Good morning"
//   ต้องได้:  Good morning

// เขียนตรงนี้ 👇



// ── โจทย์ 3 ───────────────────────────────────────────────
// บรรทัดล่างนี้มัน Error เพราะอะไร? ให้ "แก้" ให้ถูก
// (ใบ้: level เป็น number จะเอา string มาใส่ไม่ได้)
//
//   let level: number = 1;
//   level = "two";          // ❌ แก้บรรทัดนี้ให้ถูก
//
//   ต้องได้:  2

// เขียนเวอร์ชันที่ถูกตรงนี้ 👇



// ── โจทย์ 4 ───────────────────────────────────────────────
// ตัวไหนควรใช้ annotation ตัวไหนปล่อยให้ inference?
// ประกาศ 2 ตัว:
//   - ตัวแปร  discount  ที่ยังไม่รู้ค่าตอนนี้ (กำหนด type number ไว้ก่อน)
//   - ตัวแปร  shopName  ที่รู้ค่าเลยว่า "SakuraShop" (ปล่อยให้เดา)
// แล้ว log ทั้งคู่

// เขียนตรงนี้ 👇
