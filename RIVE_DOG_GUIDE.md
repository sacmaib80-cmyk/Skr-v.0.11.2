# 🐺 คู่มือทำหมา SakuraQ ให้มองตามเมาส์ด้วย Rive (ฉบับเริ่มจากศูนย์)

> เป้าหมาย: ได้ไฟล์ `www/dog.riv` ที่หมา **กลอกตา/มองตามเมาส์ + ตอบตอน hover/คลิก + หายใจ idle**
> โค้ดในแอปทำเสร็จรอแล้ว — พอวางไฟล์ `dog.riv` ลง `www/` มันทำงานเองทันที ไม่ต้องแก้โค้ดอีก
>
> **ข่าวดี:** Rive editor ใช้ฟรี, export `.riv` ฟรี, runtime ฟรี (โอเพนซอร์ส) — ไม่มีค่าใช้จ่าย
> **เวลาที่ต้องใช้จริง:** มือใหม่ ~2–4 ชม. (รวมเรียนรู้). ทำเป็นช่วงๆ ได้ ไม่ต้องรวด

---

## ภาพรวม 4 เฟส
1. **เตรียมอาร์ต** — ตัด "ตาดำ" ออกจากตัวหมา (นอก Rive, ใช้ Photopea ฟรี)
2. **ประกอบใน Rive** — import ตัวหมา + ตาดำ วางซ้อนให้ตรง
3. **ทำให้ตาขยับ** — สร้าง State Machine + inputs ตาม contract แล้วผูกให้ตาเลื่อนตามค่า
4. **Export** — เซฟเป็น `dog.riv` วางใน `www/`

> 🎯 **Contract ที่ต้องตรงเป๊ะ** (โค้ดอ่านชื่อพวกนี้):
> - State Machine ชื่อ **`Dog`**
> - Number `lookX` : 0=มองซ้าย, 50=ตรง, 100=มองขวา
> - Number `lookY` : 0=มองขึ้น, 50=ตรง, 100=มองลง
> - Boolean `isHover` : เมาส์อยู่บนตัว
> - Trigger `poke` : คลิก

---

## เฟส 1 — เตรียมอาร์ต (ตัดตาดำออก) 🎨

ตาจะขยับได้ ต้องแยก "ตาดำ" เป็นชิ้นลอย และตัวหมาต้องมี "เบ้าตาว่าง" (ทาสีขาวแทนตาดำ)
ใช้รูป **`www/mascot.png`** เป็นต้นฉบับ (หมาเต็มตัว พื้นหลังใส)

### ใช้ Photopea (ฟรี ไม่ต้องลง — เปิดในเบราว์เซอร์)
1. เปิด https://www.photopea.com → File → Open → เลือก `mascot.png`
2. **ทำเลเยอร์ "ตัวหมา (ตาว่าง)":**
   - คลิกขวาเลเยอร์ → Duplicate (กันพลาด)
   - เลือกเครื่องมือ **Clone Stamp** หรือ **Brush** สีขาวอมฟ้าอ่อน (สีเดียวกับตาขาวของหมา)
   - ระบายทับ **ตาดำ 2 ข้าง + จุดไฮไลต์** ให้กลายเป็นตาขาวโล่งๆ (เหมือนหมาหลับตาขาว)
   - เซฟ: File → Export as → PNG → ตั้งชื่อ `dog-body.png` (ขนาดเท่าเดิม)
3. **ทำเลเยอร์ "ตาดำ":**
   - กลับไปเลเยอร์ต้นฉบับ (ตายังอยู่) → ใช้ **Lasso/Elliptical Marquee** ลากเลือกเฉพาะ **ตาดำ 2 ข้าง (+จุดขาวในตา)**
   - Edit → Copy → Edit → Paste (ได้เลเยอร์ใหม่มีแต่ตาดำ พื้นหลังใส)
   - ซ่อนเลเยอร์อื่นให้เหลือแต่ตาดำ → File → Export as → PNG → ชื่อ `dog-eyes.png`
   - ⚠️ **สำคัญ:** export ที่ canvas ขนาดเดิม (อย่า crop) เพื่อให้ตาดำอยู่ "ตำแหน่งเดิม" เวลาเอาไปวางทับ

> ผลลัพธ์เฟส 1: ได้ 2 ไฟล์ → `dog-body.png` (หมาตาขาวโล่ง) + `dog-eyes.png` (ตาดำลอยๆ)
> 📌 ติดตรงไหน ส่งรูปหน้าจอมาถามได้ตลอด

---

## เฟส 2 — ประกอบใน Rive 🧩

1. สมัคร/เข้า https://rive.app (ฟรี) → กดปุ่ม **New File** → เลือก **Animate**
2. จะเห็น **Artboard** (กรอบสี่เหลี่ยม = เวที). ปรับขนาดให้พอดีหมา
3. ลากไฟล์ **`dog-body.png`** เข้า artboard → วางกลาง = ชั้นล่าง
4. ลากไฟล์ **`dog-eyes.png`** เข้า → มันจะมาทับพอดี (เพราะ export ขนาดเดิม) = ชั้นบน
5. ใน panel ซ้าย (Hierarchy) จะเห็น 2 ชั้น: `dog-eyes` อยู่ **บน** `dog-body`
6. เลือก `dog-eyes` → กด **Ctrl+G** (Group) ตั้งชื่อกลุ่มว่า `eyes` → กลุ่มนี้แหละที่เราจะขยับ

> ผลลัพธ์เฟส 2: หมากลับมาดูเหมือนเดิมเป๊ะ (ตาดำซ้อนตาขาวพอดี) และตาแยกเป็นกลุ่ม `eyes` ขยับได้

---

## เฟส 3 — ทำให้ตามองตามเมาส์ 👀 (หัวใจ)

### 3.1 สร้าง State Machine + inputs
1. ใน panel ล่าง สลับเป็นแท็บ **State Machine** → กด **+** สร้างใหม่ → **เปลี่ยนชื่อเป็น `Dog`** (เป๊ะ!)
2. ที่ panel **Inputs** (มุมนึงของ State Machine) กด **+** เพิ่มทีละตัว:
   - Number ชื่อ `lookX`
   - Number ชื่อ `lookY`
   - Boolean ชื่อ `isHover`
   - Trigger ชื่อ `poke`

### 3.2 ทำ "ตาเลื่อนตาม lookX/lookY" ด้วย Blend State (วิธีง่ายสุดสำหรับมือใหม่)
แนวคิด: สร้าง timeline สั้นๆ ที่ตาเลื่อน "ซ้ายสุด→ขวาสุด" แล้วใช้ `lookX` เป็นตัวเลื่อนหัวอ่าน

1. ไปแท็บ **Animate** (timeline) → สร้าง animation ใหม่ชื่อ `look` 
2. ที่ **frame 0**: เลือกกลุ่ม `eyes` → เลื่อนไป **ซ้ายสุด + ขึ้นสุด** นิดเดียว (ตาเหลือบมุมซ้ายบน) → กดเพิ่ม keyframe (รูปเพชร) ที่ X และ Y ของ eyes
3. เลื่อนหัวอ่านไป **frame กลาง**: เลื่อน `eyes` กลับ **ตรงกลาง** → keyframe
4. **frame สุดท้าย**: เลื่อน `eyes` ไป **ขวาสุด + ลงสุด** นิด → keyframe
   - (ระยะเลื่อนแค่ ~3–6px ก็พอ — ตาคนเรากลอกนิดเดียว ถ้าเยอะจะหลอน)
5. กลับไป **State Machine `Dog`** → ลาก animation `look` เข้าเป็น state
6. คลิก state `look` → ตั้ง **Blend = 1D**, ตัวขับ (input) = `lookX`, ช่วง 0–100
   - แบบนี้ lookX=0 → เฟรมแรก (มองซ้าย), 50 → กลาง, 100 → เฟรมท้าย (มองขวา)

> สำหรับ **lookY** (ขึ้น-ลง) ทำเพิ่มอีกชั้นแบบ **Additive Blend** ขับด้วย `lookY` (Rive รองรับ blend ซ้อน)
> ถ้างงตรงนี้ — ทำแค่ `lookX` (ซ้าย-ขวา) ให้ได้ก่อนก็ "มองตามเมาส์" แล้ว 80% ค่อยเติม Y ทีหลัง

### 3.3 idle หายใจ (ทำให้ดูมีชีวิต)
- สร้าง animation `idle` → ทำตัวหมา **scale ขึ้น-ลงนิดๆ (1.0 → 1.02 → 1.0)** วนลูป → ตั้งเป็น state เริ่มต้นของ State Machine (เล่นตลอด)

### 3.4 hover / poke (ทำทีหลังได้)
- `isHover` = true → เปลี่ยนไป state ที่หูตั้ง/ตาโต
- `poke` (trigger) → เล่น animation เด้ง 1 ที (กระโดดเล็กๆ) แล้วกลับ idle

> ผลลัพธ์เฟส 3: กด **Preview** (ปุ่ม play) ใน Rive แล้วลองลาก slider ค่า `lookX` → ตาต้องเลื่อนตาม ✅

---

## เฟส 4 — Export เป็น dog.riv 📦
1. มุมขวาบน → **Export** → เลือก **For a Game / Runtime** (ไฟล์ `.riv` ไม่ใช่ video)
2. เซฟไฟล์ → เปลี่ยนชื่อเป็น **`dog.riv`**
3. เอาไปวางที่ **`d:\git\www\dog.riv`**
4. บอกกู — กูจะ verify ให้ว่ามันโหลด, สลับจากวิดีโอมาเป็น Rive, และตามเมาส์จริง (ทดสอบด้วย headless browser)

---

## 📺 ดูคลิปประกอบ (ค้นใน YouTube)
- "Rive tutorial for beginners" — ปูพื้นฐาน editor
- "Rive state machine tutorial" — เข้าใจ inputs + states
- "Rive look at cursor" / "Rive eye follow mouse" — ตรงงานนี้เป๊ะ (มีหลายคลิปทำตา/หัวตามเมาส์)
- เอกสารทางการ: https://rive.app/docs

---

## 🆘 จุดที่มือใหม่มักติด (ถามกูได้)
- **ตาดำซ้อนไม่ตรง** → ตอน export ทั้ง 2 PNG ต้องขนาด canvas เท่ากัน (อย่า crop)
- **ชื่อไม่ตรง contract** → State Machine ต้อง `Dog`, inputs ต้อง `lookX/lookY/isHover/poke` สะกดเป๊ะ (case-sensitive)
- **export ผิดเป็นวิดีโอ** → ต้องเลือก runtime `.riv` ไม่ใช่ MP4/GIF
- **ตากลอกเยอะไปดูหลอน** → ลดระยะเลื่อน eyes ใน keyframe ให้เหลือ 3–5px

---

## สถานะฝั่งโค้ด (พร้อมแล้ว ✅)
- runtime: `www/rive.min.js` + `www/rive.wasm` (vendored)
- markup/CSS/JS: `initMascotRive()` ใน `www/index.html` — โหลด `./dog.riv`, มองตามเมาส์ (lerp ลื่น), hover/poke, idle, pause ตอนซ่อน, fallback วิดีโอถ้าไม่มีไฟล์
- service worker: cache อัปเดตแล้ว (`sakuraq-v0-17-2-rive`)
- **เหลือแค่ไฟล์ `www/dog.riv` จากมึง** → เสร็จ
