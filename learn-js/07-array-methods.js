// ============================================
// บทที่ 7 — Array Methods: map / filter / reduce
// วิธีรัน: node 07-array-methods.js
// ============================================

// ============================================
// Part 1: .map()
// ============================================
// map = วนทุกตัวใน array แล้ว return array ใหม่
// ของเดิมไม่เปลี่ยน

// const nums = [1, 2, 3, 4]
// const doubled = nums.map((n) => n * 2)
// console.log(doubled)  → [2, 4, 6, 8]

// ============================================
// Part 2: .filter()
// ============================================
// filter = กรองเอาเฉพาะที่ผ่านเงื่อนไข return true

// const nums = [1, 2, 3, 4, 5]
// const evens = nums.filter((n) => n % 2 === 0)
// console.log(evens)  → [2, 4]

// ============================================
// Part 3: .reduce()
// ============================================
// reduce = รวมทุกตัวเป็นค่าเดียว
// รับ 2 parameter: (accumulator, currentValue)
// accumulator = ค่าสะสม, currentValue = ตัวปัจจุบัน

// const nums = [1, 2, 3, 4]
// const sum = nums.reduce((acc, n) => acc + n, 0)
// console.log(sum)  → 10
//                              ↑ ค่าเริ่มต้นของ acc

// ============================================
// โจทย์ 1 — map
// ============================================
// มี array นี้:
let prices = [100, 250, 80, 400, 60]

// ให้ใช้ .map() สร้าง array ใหม่ชื่อ discounted
// โดยลดราคาทุกตัวลง 10%
// แล้ว log ผลลัพธ์
// ตัวอย่าง: 100 → 90, 250 → 225

// เขียนด้านล่าง:

const discounted = prices.map((n) => n * 0.9 )
console.log(discounted)


// ============================================
// โจทย์ 2 — filter
// ============================================
// มี array นี้:
let scores = [45, 72, 33, 88, 91, 55, 60]

// ให้ใช้ .filter() เอาเฉพาะคะแนนที่ >= 60
// เก็บไว้ในตัวแปรชื่อ passed
// แล้ว log ผลลัพธ์

// เขียนด้านล่าง:


// ============================================
// โจทย์ 3 — reduce
// ============================================
// มี array นี้:
let minutes = [30, 45, 60, 20, 90]

// ให้ใช้ .reduce() รวม minutes ทั้งหมด
// เก็บไว้ในตัวแปรชื่อ total
// แล้ว log: "รวม X นาที"

// เขียนด้านล่าง:


// ============================================
// โจทย์ 4 — รวมกัน (boss level)
// ============================================
// มี array นี้:
let quests = [
  { title: "อ่านหนังสือ", minutes: 30, done: true },
  { title: "ออกกำลังกาย", minutes: 45, done: false },
  { title: "เขียนโค้ด", minutes: 60, done: true },
  { title: "ทำสมาธิ", minutes: 20, done: true },
]

// ให้หา "รวม minutes ของเฉพาะ quest ที่ done === true"
// โดยใช้ .filter() แล้วต่อด้วย .reduce()
// แล้ว log: "เวลารวมที่ทำสำเร็จ: X นาที"

// Hint: สามารถต่อ method ได้เลย
// quests.filter(...).reduce(...)

// เขียนด้านล่าง:
