// ============================================
// บทที่ 2 — ระดับกลาง
// วิธีรัน: node 02-intermediate.js
// ============================================

// ============================================
// แบบฝึกหัด 1 — Template Literal
// ============================================
// แทนที่จะต่อ string ด้วย + ใช้ backtick ` ` แทน
// แล้วแทรกตัวแปรด้วย ${...}
//
// ตัวอย่าง:
//   let name = "กู"
//   console.log(`สวัสดี ${name}`)  // → สวัสดี กู
//
// โจทย์: สร้างตัวแปร name, level, xp
// แล้ว log ออกมาแบบนี้ (แทนค่าจริง):
// "ผู้เล่น: CEO | Level: 5 | XP: 1200"



let totalSeconds = 3725

let hours = Math.floor(3725 / 3600)   // 3725 / 3600 แล้วปัดลง
let minutes = Math.floor(3725 % 3600 / 60) // เศษที่เหลือ / 60 แล้วปัดลง
let seconds =  3725 % 60  // เศษสุดท้าย

console.log(`${hours} ชั่วโมง ${minutes} นาที ${seconds} วินาที`)
